name: example
description: A new Flutter project.

publish_to: "none"

version: 0.1.0

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  gallery_media_picker:
    path: ../
  provider:
  photo_view:
  share_plus:
  better_player: ^0.0.83

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints:

flutter:
  uses-material-design: true
